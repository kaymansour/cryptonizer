Backend does not need api key :
example: https://api.coingecko.com/api/v3/search?query=btc 
 
```bash
npm run dev

cd /home/ayoub/senior/backend && source venv/bin/activate && uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

